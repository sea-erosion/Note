# ✦ 拡張案ロードマップ

現在のツールをベースにした、今後の拡張アイデアの一覧です。  
優先度・難易度を目安に分類しています。

---

## 優先度：高（すぐ着手できる）

### エディタ強化

- **スニペット登録**  
  よく使うフレーズや定型文を登録してワンクリックで挿入する機能。セリフの語尾・擬音語などをストックしておける。

- **フォーカスモード**  
  サイドバーと右パネルを非表示にして本文エリアを最大化するモード。執筆に集中したいときに使う。

- **行間・余白の設定**  
  フォントサイズ以外に、行間（line-height）と左右の余白（padding）をスライダーで調整できるようにする。

- **段落ジャンプ**  
  `---`（区切り記号）や空行の連続などをシーン区切りとみなし、前後ジャンプするショートカット。

### 章・話管理

- **ドラッグ＆ドロップで並べ替え**  
  サイドバー上で章・話を自由に並べ替えられるようにする。現在は追加順固定。

- **話の文字数表示**  
  サイドバーの各話項目に文字数を小さく表示して、バランスを把握しやすくする。

- **章・話のメモ欄**  
  本文とは別に、各話ごとにメモ・プロット覚書を書けるフィールドを設ける。

### 保存・出力

- **epub出力**  
  epub形式でダウンロードできるようにする。電子書籍リーダーで読める形式。`epub.js` や手書きのXHTML生成で実現可能。

- **縦書きPDF出力**  
  縦書きレイアウトのままPDFとして出力。`@page` CSSと印刷ダイアログを使う簡易版からでも始められる。

- **小説投稿サイト向けテキスト変換**  
  カクヨム・なろう・pixivなどの投稿フォーマット（ルビ記法・傍点など）に合わせて出力するボタン。

---

## 優先度：中（設計が必要）

### AI支援の拡張

- **プロット生成**  
  章・話の骨格（起承転結）をAIに提案させ、構成として自動でシーンを追加するフロー。

- **一貫性チェック**  
  登場人物の設定（年齢・性格・外見）と本文の記述を比較し、矛盾を指摘するAI機能。

- **文体分析・アドバイス**  
  同じ語尾の多用・受動態の偏り・読点の少なさなどを検出してフィードバック。

- **ダイアログ補完**  
  会話文の途中でAIが続きを候補として提示する、GitHub Copilot的なインライン補完。

- **キャラクター別の口調チェック**  
  登場人物ごとに口調のサンプルを登録し、セリフがその口調から外れていないか確認。

### 統計・分析

- **執筆カレンダー**  
  日次記録をカレンダーヒートマップで視覚化する（GitHubのコントリビューションマップのような形式）。

- **章・話ごとの文字数グラフ**  
  各話の文字数を棒グラフで表示して、ボリュームのばらつきを把握。

- **語彙統計**  
  最頻出語・登場人物名の登場回数・助詞の分布などを集計して表示。

### UI・UX

- **テーマ切替**  
  ダークテーマ固定の現状から、ライトテーマ・セピアテーマなどを選択できるようにする。

- **フォント選択**  
  明朝・ゴシック・游明朝・源ノ明朝（Webフォント）などから選択できるドロップダウン。

- **キーボードショートカット一覧**  
  ヘルプオーバーレイとして `?` キーでショートカット一覧を表示する機能。

---

## 優先度：低（大規模改修）

### マルチデバイス対応

- **クラウド同期**  
  Google Drive / Dropbox APIと連携し、複数端末でデータを同期する。現状はlocalStorage限定のため端末間共有ができない。

- **PWA化**  
  Service Workerを使ってインストール可能なPWAにする。ホーム画面に追加でき、JSZipもオフラインで使えるようになる。

- **モバイル対応レイアウト**  
  画面幅に応じてサイドバーをドロワー式に切り替え、スマートフォン・タブレットでも快適に使えるようにする。

### コラボレーション

- **コメント・注釈機能**  
  本文の特定箇所に付箋感覚でコメントを貼れる機能。校正・推敲フェーズで役立つ。

- **バージョン差分表示**  
  バックアップ間の本文差分をdiff形式で可視化し、どこを変更したか確認できるようにする。

### 外部連携

- **カクヨム / なろう への直接投稿**  
  各サイトのAPIを使い、ツールから直接投稿・更新できるようにする。

- **Obsidian / Notion へのエクスポート**  
  Markdown形式でのエクスポートに対応し、ノートアプリとの連携を容易にする。

---

## 技術的な改善案

| 項目 | 現状 | 改善案 |
|------|------|--------|
| データ管理 | localStorage（5MB上限） | IndexedDBへ移行（容量制限を緩和） |
| エディタ | `<textarea>` | CodeMirrorやProseMirrorで書式付き編集に対応 |
| ZIP機能 | CDN依存 | PWA化でオフライン対応 |
| AI連携 | Anthropic API直呼び出し | ストリーミング対応（生成をリアルタイム表示） |
| テスト | なし | 単体テストを整備してリグレッションを防ぐ |

---

## 実装難易度まとめ

| 難易度 | 拡張案（抜粋） |
|--------|---------------|
| ★☆☆ 簡単 | フォーカスモード、行間設定、話の文字数表示、フォント選択 |
| ★★☆ 中程度 | ドラッグ並べ替え、epub出力、執筆カレンダー、PWA化 |
| ★★★ 難しい | クラウド同期、コラボレーション、投稿サイト連携、AIダイアログ補完 |
